<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_splash"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primary"
    tools:context="udacity.com.capstone.activities.SplashActivity">

    <ImageView
        android:id="@+id/icon"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" android:contentDescription="@string/conent_des_splash"
        android:layout_marginTop="@dimen/dimen_60dp"
        android:src="@drawable/ic_restore" />

    <TextView
        android:id="@+id/txt_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/icon"
        android:layout_centerHorizontal="true"
        android:text="@string/app_name"
        android:textAllCaps="true"
        android:textColor="@color/icons"
        android:textSize="@dimen/dimen_40sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/txt_app_version"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" android:padding="@dimen/dimen_16dp"
      android:textSize="@dimen/dimen_18sp"
        android:layout_centerHorizontal="true"
        android:text="@string/app_version" android:layout_alignParentBottom="true"
        android:textAllCaps="true"
        android:textColor="@color/icons"

   />
</RelativeLayout>
